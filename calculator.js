const fetchNumber=require("../api/fetchNumber");
import url from 'node:url';

 const calculator=express("calculator");
const handleRequest=async(req,res)=>{
       const  parsed=url.parse(req,res,true);
       const category=parseuRL
       }
}